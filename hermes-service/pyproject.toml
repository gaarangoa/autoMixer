[project]
name = "automixer-hermes-service"
version = "0.1.0"
description = "Bridge sidecar: drives the Hermes agent over ACP and exposes an HTTP/SSE chat surface to AutoMixer."
requires-python = ">=3.11"
dependencies = [
    "agent-client-protocol>=0.9.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]
